#!/bin/bash
# migrate.sh — merge_bundle for `apply --from` (staged extraction, path
# validation via deepest-existing-ancestor — BSD-safe, no realpath -m).
# Lands in M4.

merge_bundle() { die "merge_bundle: not implemented yet (M4)" 2; }

#!/bin/bash
# overlay.sh — company overlay resolution and layering. Lands in M5.
# Hooks run only after one-time registration + per-overlay opt-in.

resolve_overlay() { die "resolve_overlay: not implemented yet (M5)" 2; }

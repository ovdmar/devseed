#!/bin/bash
# export.sh — migration bundle builder (umask 077, 0600 bundle, manifest).
# Lands in M4. Category flags: --only-categories/--except-categories.

cmd_export() { die "devseed export: not implemented yet (M4)" 2; }

#!/bin/bash
# capture.sh — cmd_capture orchestration, --add, --prune, --check alias.
# Lands in M1 (--check alias wires to cmd_diff in M2).

cmd_capture() { die "devseed capture: not implemented yet (M1)" 2; }

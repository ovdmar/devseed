# secrets.list — included ONLY with `devseed export --include-secrets`.
# Bundles are plain tar in v1 (vision.md ADR-3): written 0600 under
# ~/.devseed/bundles/, but NOT encrypted — treat the file accordingly.
.ssh/**
.gnupg/**
.aws/credentials
.netrc

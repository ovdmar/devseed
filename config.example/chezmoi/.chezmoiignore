# .chezmoiignore — paths chezmoi must never manage.
# The block below is (re)generated by `devseed capture` from exclusions.txt;
# lines OUTSIDE the block are yours and are preserved.
# >>> devseed managed (generated from exclusions.txt) >>>
# <<< devseed managed <<<

[defaults]
nocows = True
retry_files_enabled = False
display_skipped_hosts = False
host_key_checking = False
interpreter_python = auto_silent

[privilege_escalation]
# The bootstrapper decides whether a become password is needed (sudo -n
# probe); when it is, it arrives via --become-password-file on an fd.
become_ask_pass = False
